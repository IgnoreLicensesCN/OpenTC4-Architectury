package thaumcraft.api.research.render;

import net.minecraft.resources.ResourceLocation;

public class SimpleShownIconForeground extends ShownIconForeground {
    ResourceLocation icon;
    public SimpleShownIconForeground(ResourceLocation icon) {
        this.icon = icon;
    }
}
