package thaumcraft.api.expands.listeners.researchtable.consts;

import thaumcraft.api.expands.listeners.researchtable.listeners.WriteAspectBeforeListener;

public enum WriteAspectBeforeListenerEnums {
    ;
    
    public final WriteAspectBeforeListener listener;
    WriteAspectBeforeListenerEnums(WriteAspectBeforeListener listener) {
        this.listener = listener;
    }
}
